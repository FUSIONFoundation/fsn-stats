CREATE TABLE blocks (
    height          BIGINT,
    blocktime       DOUBLEPRECISION,
    difficulty      BIGINT,
    transactions    BIGINT,
    gasspending     BIGINT,
    gaslimit        BIGINT,
    ticketnumber    BIGINT,
    unclecount      BIGINT,
    uncles          BIGINT
);
