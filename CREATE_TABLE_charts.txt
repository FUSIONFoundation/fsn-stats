CREATE TABLE charts (
    x               DOUBLEPRECISION,
    dx              DOUBLEPRECISION,
    y               DOUBLEPRECISION,
    frequency       DOUBLEPRECISION,
    cumulative      DOUBLEPRECISION,
    cumpercent      DOUBLEPRECISION,
    ticketnumber    DOUBLEPRECISION,
);
