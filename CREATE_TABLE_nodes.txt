CREATE TABLE nodes (
    id          TEXT,
    datetime    TIME,
    block       BIGINT,
    mining      BOOLEAN,
    syncing     BOOLEAN,
    peers       BIGINT,
    uptime      BIGINT,
    latency     BIGINT,
    tickets     BIGINT,
    PRIMARY KEY (id)
);
